# Infrastructure (Docker)

This folder contains Docker-related artifacts for local and CI environments.

- `docker-compose.dev.yml`: development services wiring (postgres, redis, api, web)
- Dockerfiles can live here over time if you want infra-only ownership.
