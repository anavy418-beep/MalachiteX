# Root Prisma Folder

This folder is reserved for monorepo-level Prisma assets (migrations, schema federation, tooling).

Current API schema and seed live in `apps/api/prisma`.
If you centralize Prisma later, move schema/seed here and update scripts accordingly.
