# API src/prisma

Placeholder Prisma integration boundary at `apps/api/src/prisma`.

Current implementation uses `apps/api/src/common/prisma`.
You can migrate to this location gradually and keep module imports stable.
